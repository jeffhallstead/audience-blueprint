# Move screen recording script to a visible project folder

## Goal
Make the finalized screen recording script easy to find in the project file tree by moving it out of the hidden `.lovable/plan/` directory into a visible `docs/` file.

## Change
1. Copy `.lovable/plan/screen-recording-script-publisher-blueprint-launch-share-2026-08-06.md` to `docs/screen-recording-script-publisher-blueprint-launch-share.md`.
2. Keep the original archived plan file in place (no deletion).
3. No code, routing, or build changes are required.

## Verification
- Read the new `docs/screen-recording-script-publisher-blueprint-launch-share.md` file and confirm it matches the original content.
- Confirm it appears in the project file tree under `docs/`.
