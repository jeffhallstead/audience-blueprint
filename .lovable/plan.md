Generate a downloadable Markdown copy of the product specification

1. Copy the existing repo file `PRODUCT-SPEC.md` to `/mnt/documents/Publisher-Blueprint-Product-Specification-v1.0.md` so it appears in the user files panel.
2. Verify the exported file exists and has the expected 434-line length.
3. Emit a `<presentation-artifact>` tag pointing to the exported file.