# Update Airtable template with public share link

## Goal

Incorporate the newly generated public Airtable template share URL into the repository documentation.

## Deliverables

- Update `api/airtable-template.md`:
  - Add the public shareable URL `https://airtable.com/appYrnFpbA3XUcXcK/shr9eae60oPEAb6xT` under the **Airtable base link** section.
  - Clarify that the link is read-only and allows viewers to copy the base.
- Update `README.md`:
  - Add a link to the public Airtable template in the integrations section.
- Verify the link resolves correctly.

## Steps

1. Read `api/airtable-template.md` and replace the private base URL with the public share URL.
2. Read `README.md` and add a reference to the public Airtable template.
3. Verify the public share URL is reachable.

## Success criteria

- `api/airtable-template.md` contains the public shareable URL.
- `README.md` references the public template.
- No broken links or stale references remain.